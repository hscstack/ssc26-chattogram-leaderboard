# Project Context
- Static web project (HTML, CSS, JS).
- No build system, bundler, or package manager present.
- Empty files: `app.js`, `index.html`, `style.css`.
- Data source: `data/students.json` contains student records (name, mark, gpa, status, school).

# Commands
- No test, lint, or build commands.

# Guidelines
- Write vanilla HTML/CSS/JS.
- Do not introduce dependencies unless requested.
